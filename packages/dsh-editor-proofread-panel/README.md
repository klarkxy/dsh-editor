# dsh-editor-proofread-panel

Desktop-only manuscript proofread seat. Host is a no-op; the Client contributes `dsh-editor.sidebar.tools` and the `proofread-document` / `proofread-manuscript` commands. Scanning stays on `/dsh-editor-workbench` `proofread.scan`. Author confirmation stays on the Shell `ProposalCard`.
