# dsh-editor-overview-panel

Desktop-only work overview seat. Host is a no-op; the Client contributes `dsh-editor.center.overlays` and the `overview` command (Ctrl+Shift+O). Overview data stays on `/dsh-editor-workbench` `project.overview`, `progress.history`, and `chapter.statusSet`.
