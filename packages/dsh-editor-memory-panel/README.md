# dsh-editor-memory-panel

Desktop-only project memory seat. Host is a no-op; the Client contributes `dsh-editor.sidebar.tools` and the `memory-open` command. Records stay on `/dsh-editor-workbench` `memory.list` / `memory.get` / `memory.apply` / `memory.undo`. Chat receipts still render in the Shell.
