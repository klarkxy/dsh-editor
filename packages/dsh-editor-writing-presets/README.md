# dsh-editor-writing-presets

桌面私有的第一方写作模式 preset 包：`presets/` 提供 `dsh-editor-article`（文章与自媒体）与 `dsh-editor-technical`（技术文档）两套对话 preset，经 `dshEditor.presets` 声明，由桌面 profile 模板通道部署到 `<dshHome>/.agent-presets/`。作者可在设置「插件 → 写作模式」里开关；进行中的对话不受影响。本包没有 Cordis 入口，永远不会作为插件加载。
